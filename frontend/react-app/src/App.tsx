import React from "react";
import UploadForm from "./components/UploadForm";

const App: React.FC = () => {
  return (
    <div style={{ padding: "40px" }}>
      <UploadForm />
    </div>
  );
};

export default App;