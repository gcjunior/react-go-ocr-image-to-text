services:

  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      FRONTEND_URL: http://localhost:3000

  frontend:
    build:
      context: ./frontend/react-app
    ports:
      - "3000:80"
    depends_on:
      - backend